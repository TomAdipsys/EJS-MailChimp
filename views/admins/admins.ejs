<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Admins</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
</head>
<body>
    <%- include('../partials/navbar', { user, csrfToken, activePage }) %>

<h1>Liste des admins</h1>
<a href="/admins/new">Créer un nouvel admin</a>
<ul>
  <% admins.forEach(admin => { %>
    <li>
      <%= admin.email %> - <%= admin.role %> -
      <a href="/admins/<%= admin.id %>/edit">Modifier</a>
      <form action="/admins/<%= admin.id %>?_method=DELETE" method="post" style="display:inline">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <button type="submit">Supprimer</button>
      </form>
    </li>
  <% }) %>
</ul>

<%- include ('../partials/footer') %>
</body>
</html>
