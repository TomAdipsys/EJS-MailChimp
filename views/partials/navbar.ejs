<nav class="navbar is-light mb-5" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <a class="navbar-item" href="/dashboard">
      <strong>HMCHIMP</strong>
    </a>
  </div>
  <div class="navbar-menu">
    <div class="navbar-start">
      <a class="navbar-item <%= activePage === 'dashboard' ? 'is-active' : '' %>" href="/dashboard">Dashboard</a>
      <a class="navbar-item <%= activePage === 'profile' ? 'is-active' : '' %>" href="/profile">Mon profil</a>
      <% if (user && user.role === 'SUPERADMIN') { %>
        <a class="navbar-item <%= activePage === 'admins' ? 'is-active' : '' %>" href="/admins">Admin</a>
      <% } %>
    </div>
    <div class="navbar-end">
      <div class="navbar-item">
        <form action="/logout" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <button class="button is-light" type="submit">Déconnexion</button>
        </form>
      </div>
    </div>
  </div>
</nav>